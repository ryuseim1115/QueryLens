"""
ルーターの責務は、HTTP固有の型(UploadFile, Request, pydanticスキーマなど)を
FastAPI/Starletteに依存しない素の値(int, str, bytesなど)に変換して
サービス層に渡すこと。逆に、サービス層の返り値をHTTPレスポンスの形
(ステータスコード、JSONなど)に組み立てるのもルーターの仕事。

これにより、サービス層はどこから呼ばれても(CLIやバッチ処理からでも)
動作できる、Webフレームワークに依存しない形を保つ。
"""
